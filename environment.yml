name: ai-rag-chatbot-learning
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - streamlit
      - langgraph
      - langchain
      - langchain-core
      - langchain-google-genai
      - python-dotenv
      - langgraph-checkpoint-sqlite